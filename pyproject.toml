[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pelak-khan"
version = "0.5.0"
description = "Iranian license plate recognition application"
requires-python = ">=3.10,<3.14"

[tool.setuptools.packages.find]
where = ["src"]
